# Build scripts pnpm is allowed to run on install (pnpm blocks them by default).
# Prisma needs its engine build/generate; esbuild and unrs-resolver compile native binaries.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  unrs-resolver: true
